services:
  - type: web
    name: validador-assinatura-icp
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8080
      - key: JAVA_TOOL_OPTIONS
        value: -Dfile.encoding=UTF-8
      - key: MAX_FILE_SIZE
        value: 50MB
